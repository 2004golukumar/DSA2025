class Solution {
public:
    int singleNonDuplicate(vector<int>& nums) {
        //if nums=1
        int n=nums.size();
        if(n==1) return nums[0];
        //if [0]!=[1]
        if(nums[0]!=nums[1]) return nums[0];
        //if[n-2]!=[n-1]
        if(nums[n-1]!=nums[n-2]) return nums[n-1];
…        //right side eleminate right element
        else{
            high=mid-1;
        }
        }
      return -1;
    }
};
